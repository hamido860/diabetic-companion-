import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.diabeticcompanion2.app',
  appName: 'Diabetic Companion 2',
  webDir: 'dist'
};

export default config;
